import React from 'react';

/**
 * PUBLIC_INTERFACE
 * Room selection: grid of cards (show available/booked with icons).
 */
export default function RoomPage() {
  return (
    <div className="rooms-page">
      <h2>Room Selection</h2>
      <p>Pick and manage your group's stay.</p>
    </div>
  );
}
