import React from 'react';

/**
 * PUBLIC_INTERFACE
 * Events list/calendar, RSVP modal, add-to-schedule.
 */
export default function EventPage() {
  return (
    <div className="events-page">
      <h2>Events</h2>
      <p>See scheduled activities, RSVP, and join the fun.</p>
    </div>
  );
}
