import React from 'react';

/**
 * PUBLIC_INTERFACE
 * Profile/settings: notification email toggles, theme, language.
 */
export default function SettingsPage() {
  return (
    <div className="settings-page">
      <h2>Settings</h2>
      <p>Profile, theme, notification, language settings.</p>
    </div>
  );
}
